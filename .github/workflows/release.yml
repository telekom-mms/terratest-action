---
name: release

on:
  push:
    branches:
      - main

jobs:
  call-release:
    uses: telekom-mms/.github/.github/workflows/release.yml@0915e4a25cc07af95b7039c3539df1791ac9d909 # main
    secrets:
      GH_BRANCH_PROTECTION_APP_TOKEN: ${{ secrets.GH_BRANCH_PROTECTION_APP_TOKEN }}
