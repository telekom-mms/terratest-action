---
name: release

on:
  push:
    branches:
      - main

jobs:
  call-release:
    uses: telekom-mms/.github/.github/workflows/release.yml@a0037962ced6a1ff0196a8148364b156b3f13596 # main
    secrets:
      GH_BRANCH_PROTECTION_APP_TOKEN: ${{ secrets.GH_BRANCH_PROTECTION_APP_TOKEN }}
