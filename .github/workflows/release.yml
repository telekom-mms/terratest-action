---
name: release

on:
  push:
    branches:
      - main

jobs:
  call-release:
    uses: telekom-mms/.github/.github/workflows/release.yml@4f66e7ff513e18bbec221f7633a1c8164fa5ea2a # main
    secrets:
      GH_BRANCH_PROTECTION_APP_TOKEN: ${{ secrets.GH_BRANCH_PROTECTION_APP_TOKEN }}
