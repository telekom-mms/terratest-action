---
name: release

on:
  push:
    branches:
      - main

jobs:
  call-release:
    uses: telekom-mms/.github/.github/workflows/release.yml@aaafe85a5c5b102395a7943820c5168c100caa16 # main
    secrets:
      GH_BRANCH_PROTECTION_APP_TOKEN: ${{ secrets.GH_BRANCH_PROTECTION_APP_TOKEN }}
